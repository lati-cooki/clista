{
  "name": "clista-ai-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The live ClisTa Protocol cockpit — app.clista.ai",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test 'test/*.test.js'",
    "test:workers": "vitest run",
    "test:all": "npm test && npm run test:workers",
    "sync:engine": "node scripts/vendor-engine.mjs",
    "sync:engine:check": "node scripts/vendor-engine.mjs --check"
  },
  "dependencies": {
    "js-sha256": "^0.11.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.18",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "vitest": "^4.1.9"
  }
}
